server:
  port: 9999

logging:
  level:
    root: WARN
    org.springframework.web: INFO
    org.springframework.security: INFO

spring:
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/any?useUnicode=true&characterEncoding=utf8mb4
    driver-class-name: org.mariadb.jdbc.Driver
    username: cj
    password: 123

mybatis:
  configuration:
    map-underscore-to-camel-case: true   #开启驼峰命名转换
